<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="shmuly.sternbach.custommishnahlearningprogram">

    <application
        android:allowBackup="true"
        android:name=".ReviewApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CustomMishnahLearningProgram">
        <activity android:name=".activities.HomePageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.CreateProgramActivity"/>
        <activity android:name=".activities.ActivityTimeline"/>
        <activity android:name=".activities.ActivityTodayList"/>
    </application>

</manifest>